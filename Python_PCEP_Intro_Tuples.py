#four ways of writing tuples
empty_tuple = ()
one_el_tuple_a = (1,)
one_el_tuple_b = 1,
three_el_tuple = 1, 2, 3,
print(three_el_tuple)
# Output:
# (1, 2, 3)

# Tuples prefer round brackets, while lists prefer square brackets
# Both type of collections can have many elements separated by commas
# Mutable data can be freely updated anytime you want
# Two types of python data: mutable and immutable data
# List are a good example of mutable data
# When you create a list, you can easily add new elements to it, delete unwanted elements swap the places

user_data = ('Talhahshah', 'Talhahstan', 1990)
user_data = ('Rahmansultan', 'Rahmannation', 2000)





